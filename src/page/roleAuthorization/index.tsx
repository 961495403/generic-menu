import React, { memo } from "react";
import { IroleAuthorizationProps } from "./props";

const RoleAuthorization: React.FC<IroleAuthorizationProps> = ({content}) => {
    return (
        <div>{333}</div>
    )
}
export default memo(RoleAuthorization)